import { defineMessages } from 'react-intl';

export const scope = 'add_to_cart';

export default defineMessages({
  add_to_cart: {
    id: `${scope}.add_to_cart`,
    defaultMessage: 'Add to cart'
  }
});
