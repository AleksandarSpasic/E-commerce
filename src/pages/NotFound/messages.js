import { defineMessages } from 'react-intl';

export const scope = 'not_found';

export default defineMessages({
  helmet: {
    id: `${scope}.helmet`,
    defaultMessage: 'Page Not Found'
  },
  button: {
    id: `${scope}.button`,
    defaultMessage: 'Go back'
  }
});
