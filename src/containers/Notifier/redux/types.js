const KEY = '[Notifi]';

const types = {
  ENQUEUE_SNACKBAR: `${KEY} ENQUEUE_SNACKBAR`,
  CLOSE_SNACKBAR: `${KEY} CLOSE_SNACKBAR`,
  REMOVE_SNACKBAR: `${KEY} REMOVE_SNACKBAR`
};

export default types;
